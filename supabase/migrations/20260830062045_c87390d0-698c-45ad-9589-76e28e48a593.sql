DROP POLICY "Admin can manage all services" ON public.services;
CREATE POLICY "Admin can manage all services" ON public.services FOR ALL TO authenticated USING (has_role(auth.uid(), 'admin'::app_role)) WITH CHECK (has_role(auth.uid(), 'admin'::app_role));